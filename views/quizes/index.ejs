<form action="/quizes">
Busqueda:<input type="text" name="search" placeholder="Cadena a buscar">
<input type="submit" value="Buscar">
</form>



<table>
<% var i; for (i=0; i < quizes.length; i++) { %>
  <tr>
    <td align="left">
        <%= "[ " +quizes[i].tema.toUpperCase().substring(0,3) + ". ]" %>
      </td>
    <td align="left"><a href="quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></a></td>
    <td align="left"><a href="quizes/<%= quizes[i].id %>/edit"><button>editar</button></a></td>
    <td align="left">
      <form method="post" action="quizes/<%= quizes[i].id %>?_method=delete">
        <button type="submit" onClick="return confirm('Borrar: <%= quizes[i].pregunta %>');">
        borrar
        </button>
      </form>
    </td>
  </tr>
  
<% } %>
</table><p>
<a href="/quizes/new"><button type="button">Crear pregunta</button></a>
